FUNCTION "current_date-1"( ) RETURNS out_current_date NVARCHAR(8)
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER AS
BEGIN
    SELECT TO_NVARCHAR(ADD_DAYS(CURRENT_DATE,-1),'YYYYMMDD') INTO out_current_date FROM DUMMY;
END;