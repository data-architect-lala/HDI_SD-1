FUNCTION "current_date"( ) RETURNS out_current_date NVARCHAR(8)
    LANGUAGE SQLSCRIPT
    SQL SECURITY INVOKER AS
BEGIN
    SELECT TO_NVARCHAR(ADD_DAYS(CURRENT_DATE,-0),'YYYYMMDD') INTO out_current_date FROM DUMMY;
END;